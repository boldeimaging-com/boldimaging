{
  "name": "boldimaging",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node bin/check-seo.mjs",
    "build:only": "astro build",
    "check:seo": "node bin/check-seo.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "types": "wrangler types",
    "deploy": "astro build && wrangler deploy -c dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.0",
    "astro": "^7.3.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
